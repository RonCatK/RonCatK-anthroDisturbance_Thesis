name: Validation Full

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  full-validation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Setup R toolchain
        uses: r-lib/actions/setup-r@v2
      - name: Placeholder full validation
        run: echo "TODO: trigger full DVC validation run"
