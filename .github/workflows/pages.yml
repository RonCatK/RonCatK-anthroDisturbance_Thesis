name: Publish Docs

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  render-and-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Setup R toolchain
        uses: r-lib/actions/setup-r@v2
      - name: Placeholder render
        run: echo "TODO: render dashboard and thesis docs"
      - name: Placeholder deploy
        run: echo "TODO: configure GitHub Pages deployment"
