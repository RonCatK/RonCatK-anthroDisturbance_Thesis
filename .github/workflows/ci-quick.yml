name: CI Quick

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  smoke-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Setup R toolchain
        uses: r-lib/actions/setup-r@v2
      - name: Placeholder check
        run: echo "TODO: add smoke tests for minimal AOI"
